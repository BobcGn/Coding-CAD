# 待办 / Todo

- 增加 Python、Java 和 Go 的专用 module/dependency detector。
- 在后续阶段评估 TypeScript AST 适配器，但保持证据层与 Architecture IR 分离。
- 增加 monorepo package boundary 识别和 package-level project mapping。
- 由独立 `implementation-validator` 消费检测结果完成架构合规验证。

- Add specialized module/dependency detectors for Python, Java, and Go.
- Evaluate a TypeScript AST adapter later while keeping evidence separate from Architecture IR.
- Add monorepo package-boundary detection and package-level project mapping.
- Let a separate `implementation-validator` consume detection results for architecture compliance.
