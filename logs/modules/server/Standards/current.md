# 当前实施标准 / Current Implementation Standards

更新时间 / Updated at: 2026-08-08 18:41 CST (Asia/Shanghai)

- Server 不拥有 Architecture IR 语义。
- Server 只编排 `dsl`、`validator` 和未来 `agent-runtime`。
- API 引入前必须先稳定包级契约。

- Server does not own Architecture IR semantics.
- Server only orchestrates `dsl`, `validator`, and future `agent-runtime`.
- Package-level contracts must stabilize before APIs are introduced.
