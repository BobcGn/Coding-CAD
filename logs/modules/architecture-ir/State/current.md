# 当前状态 / Current State

更新时间 / Updated at: 2026-08-08 21:42 CST (Asia/Shanghai)

状态：已验证。

Status: Verified.

第一版已拆分为 `common`、`project`、`domain`、`architecture`、`component`、`contract`、`constraint`、`decision`、`evolution` 和 `architecture-project`。`ArchitectureProject` 是正式入口，旧 `ArchitectureIR` 仅保留兼容类型。

The first version is split into `common`, `project`, `domain`, `architecture`, `component`, `contract`, `constraint`, `decision`, `evolution`, and `architecture-project`. `ArchitectureProject` is the formal entry point, while old `ArchitectureIR` remains only as a compatibility type.
