# 当前实施标准 / Current Implementation Standards

更新时间 / Updated at: 2026-08-08 21:42 CST (Asia/Shanghai)

- DSL 描述架构意图，不描述部署配置。
- Generator 输出字段顺序必须稳定。
- Parser 错误必须使用人类可定位的 YAML path。
- DSL 输出必须是标准 `ArchitectureProject`。

- DSL describes architecture intent, not deployment configuration.
- Generator field order must remain stable.
- Parser errors must use human-locatable YAML paths.
- DSL output must be standard `ArchitectureProject`.
