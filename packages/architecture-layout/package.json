{
  "name": "@coding-cad/architecture-layout",
  "version": "0.1.0",
  "private": true,
  "description": "Pure TypeScript projection from Architecture IR semantics to layout results.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm test:unit && pnpm test:integration",
    "test:unit": "tsc -p tsconfig.json && node dist/compiler/passes/passes.test.js && node dist/engines/elk/elk-adapter.test.js",
    "test:integration": "tsc -p tsconfig.json && node dist/compiler/compiler.test.js && node dist/engines/elk/elk-engine.test.js && node dist/incremental/incremental-layout.test.js && node dist/ghost/ghost-layout.test.js && node dist/engines/elk/performance-budget.test.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@coding-cad/architecture-ir": "workspace:*",
    "elkjs": "0.12.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
