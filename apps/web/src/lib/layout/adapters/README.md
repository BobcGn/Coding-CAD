# Layout UI Adapters / 布局 UI 适配器

未来只负责将 `LayoutResult` 转换为 Svelte Flow node/edge。布局算法与 solver adapter 必须留在 `@coding-cad/architecture-layout`；当前不包含实现。

This boundary will only convert `LayoutResult` into Svelte Flow nodes/edges. Layout algorithms and solver adapters must remain in `@coding-cad/architecture-layout`; currently contains no implementation.
